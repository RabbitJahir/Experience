laravel is mainly backend. 

> - install php  `php -v`, the language
> - isntall composer `composer -v`, the cmd to use like npm, npx, pip
> - setup database
>   - XAMPP (quick and easy backend)
>   - LAMP (for linux, tighter backend)

> making projects  
> `composer create-project laravel/laravel `myproject`