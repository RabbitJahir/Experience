# F-Droid

> `termux`, `termux-api`

### Line Wrap

> Run this:
>```
> mkdir -p ~/.termux
> exho "terminal-transcript-rows=2000" >> ~/.termux/termux.properties
> echo "set wrap" >> ~/.vimrc
> echo "set softwrap" >> ~/.nanorc
> termux-reload settings
>```
