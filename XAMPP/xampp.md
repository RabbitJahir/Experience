## XAMPP

XAMPP is a development environment package.

It includes:

```
XAMPP
 |
 ├── Apache       (Web server)
 ├── MariaDB      (Database)
 ├── PHP          (Backend language)
 └── phpMyAdmin   (Database GUI)
```

```
To use maria DB, 
Module Apache and mysql should be started
```